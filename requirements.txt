# Installs the package in editable mode along with its runtime dependencies,
# which are declared once in pyproject.toml so the two cannot drift apart,
# plus the test dependencies.
#
# On a CPU-only machine, install torch first from PyTorch's CPU index to
# avoid pulling the multi-gigabyte CUDA build:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
-e .[dev]
